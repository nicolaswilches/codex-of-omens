---
interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="rounded-md border border-ink-150 bg-paper-dark p-4 text-center">
  <p class="text-2xl font-semibold text-ink-900">{value}</p>
  <p class="mt-1 text-xs text-ink-500">{label}</p>
</div>

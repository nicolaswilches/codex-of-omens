---
interface Props {
  title?: string;
  id?: string;
}

const { title, id } = Astro.props;
---

<section id={id} class="mx-auto max-w-5xl px-6 py-16">
  {title && (
    <h2 class="mb-8 text-2xl font-bold text-navy-900">{title}</h2>
  )}
  <slot />
</section>
